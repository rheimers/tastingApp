import React from "react";
import FilterList from "../components/FilterList";

export default {
  title: "FilterList",
  component: FilterList,
};

const Template = () => <FilterList />;

export const BeverageFilterList = Template.bind({});
