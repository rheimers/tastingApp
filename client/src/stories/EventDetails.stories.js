import React from "react";
import EventDetails from "../components/EventDetails";

export default {
  title: "EventDetails",
  component: EventDetails,
};

const Template = () => <EventDetails />;

export const EventDetailsBox = Template.bind({});
