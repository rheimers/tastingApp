import React from "react";
import Main from "../components/Main";

export default {
  title: "Main",
  component: Main,
};

const Template = () => <Main />;

export const MainHome = Template.bind({});
