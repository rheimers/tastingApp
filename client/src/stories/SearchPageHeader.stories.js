import React from "react";
import SearchPageHeader from "../components/SearchPageHeader";

export default {
  title: "SearchPageHeader",
  component: SearchPageHeader,
};

const Template = () => <SearchPageHeader />;

export const SearchHeader = Template.bind({});
